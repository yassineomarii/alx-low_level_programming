#include <stdio.h>

/**
 * main - prints numbers from 0 to 9.
 *
 * Return: 0 Always success.
 */

int main(void)
{
	int i;

	for (i = 122; i >= 97; i--)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
